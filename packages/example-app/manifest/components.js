// packages/example-app/manifest/components.js
// Этот файл регистрирует все UI-компоненты (HTML-шаблоны и их стили),
// которые движок может использовать для построения интерфейса.

module.exports = {
  // --- Макеты (Layouts) ---
  // Это самые крупные "обертки" для страниц.

  // Главный макет приложения, содержащий `<html>`, `<head>`, `<body>`.
  "mainLayout": "main-layout.html",
  
  // Макет для страниц аутентификации (вход/регистрация).
  // Он будет вставляться в `mainLayout` и содержит общую
  // стилизацию для карточки с формой.
  "authLayout": { 
    "template": "auth-layout.html" 
  },

  // --- Страницы (Pages) ---
  // Это крупные блоки контента, которые вставляются в макеты.

  // Главная страница кассира, определяющая расположение списка товаров и чека.
  "cashierPage": {
    "template": "cashier-page.html"
  },

  // --- Компоненты (Components) ---
  // Это мелкие, переиспользуемые "строительные блоки".

  // Форма для входа в систему.
  "loginForm": { 
    "template": "login-form.html"
  },
  
  // Форма для регистрации нового пользователя.
  "registerForm": { 
    "template": "register-form.html" 
  },

  // Компонент для отображения чека.
  // У него есть и шаблон, и свой изолированный CSS-файл.
  "receipt": { 
    "template": "receipt.html", 
    "style": "receipt.css" 
  },
  
  // Компонент для отображения списка товаров и поиска.
  // Также имеет свой собственный изолированный CSS-файл.
  "positionsList": { 
    "template": "positionsList.html", 
    "style": "positionsList.css" 
  }
};