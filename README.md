# axleLLM Engine v2.0: The Symbiotic Framework

**axleLLM** — это декларативный движок на базе Node.js и Electron для создания нативных, кроссплатформенных десктопных приложений. Его архитектура специально спроектирована для симбиотической работы **Человека и Искусственного Интеллекта (LLM)**, смещая парадигму с написания кода на проектирование интеллектуальных, самоанализирующих систем.

С `axleLLM` все приложение — от источников данных и UI-компонентов до интеграции с ОС и бизнес-логики — определяется в едином, централизованном файле `manifest.js`. Этот подход превращает LLM из простого генератора кода в полноценного **системного архитектора**, который проектирует, отлаживает и расширяет надежные и безопасные приложения.

*   **Репозиторий:** [https://github.com/Xzdes/axleLLM](https://github.com/Xzdes/axleLLM)
*   **NPM Пакет:** [https://www.npmjs.com/package/axle-llm](https://www.npmjs.com/package/axle-llm)

---

## 🎯 Ключевые Особенности v2.0

*   **Декларативная Архитектура:** Всё приложение описывается как единый, легко читаемый объект в `manifest.js`.
*   **Интеллектуальный Валидатор:** Встроенная система "супер-валидации" проверяет всю архитектуру манифеста *до* запуска. **Благодаря Схемам Компонентов, она понимает контракты данных** и предотвращает ошибки несоответствия данных еще на этапе проектирования.
*   **Полный Жизненный Цикл:** Поддержка разработки (`dev`), тестирования (`test`) и сборки (`package`) одной командой.
*   **Real-time UI:** Встроенный WebSocket-движок для автоматического обновления интерфейса при изменении данных.
*   **Декларативная Темизация:** Глобальное управление внешним видом приложения через CSS-переменные в манифесте.
*   **Интеллектуальный Нативный Мост:** Самая мощная часть движка. Безопасный, контролируемый "белым списком", способ вызывать API операционной системы и кастомные Node.js модули.
    *   **Клиентский Мост:** Управляет UI операционной системы (диалоги, shell).
    *   **Серверный Мост:** Интегрирует любую сложную Node.js логику (работа с файлами, оборудованием, сторонними API).
    *   **Интерактивный Режим:** Позволяет `action`-ам на сервере **останавливаться, запрашивать данные у пользователя** через нативный диалог и **продолжать выполнение** с полученным результатом.
*   **Декларативная Отладка:** Шаги `log` и `log:value` позволяют LLM-агенту **самостоятельно инспектировать и отлаживать** свою же логику, анализируя состояние системы в любой точке выполнения `action`.

---

## 📖 Архитектурный План: `manifest.js`

`manifest.js` — это мозг и душа любого `axleLLM` приложения.

### Основные Секции

| Секция | Описание |
| :--- | :--- |
| **`launch`** | Конфигурация главного окна приложения. |
| **`themes`** | Декларативная система темизации. |
| **`globals`** | Глобальные переменные для UI. |
| **`auth`** | Настройки встроенной системы аутентификации. |
| **`sockets`** | Конфигурация WebSocket-каналов. |
| **`connectors`** | Декларация всех источников данных (`wise-json-db`, `in-memory`). |
| **`components`** | Реестр UI-компонентов (HTML, CSS) и их **Схем Данных (`schema`)**. |
| **`bridge`** | "Белый список" всех функций ОС и кастомных серверных модулей, доступных приложению. |
| **`routes`** | Мозг приложения. Связывает URL и `action`-ы с UI и бизнес-логикой. |

### Бизнес-логика через `steps`

Бизнес-логика описывается как последовательный массив `steps`.

**Доступные Шаги:**

| Шаг | Описание |
| :--- | :--- |
| `{ "set": "path", "to": "expr" }` | Присваивает значение переменной в контексте. |
| `{ "if": "cond", "then": [...], "else": [...] }` | Условное ветвление логики. |
| `{ "run": "script" }` | Выполняет JS-файл из `app/actions/` для сложной логики (с побочными эффектами). |
| `{ "run:set": "path", "handler": "script", "with": [...] }` | Выполняет "чистую функцию" из `app/actions/` и сохраняет возвращенное значение. |
| `{ "action:run": { "name": "route" } }` | Вызывает другой `action`-роут, позволяя создавать переиспользуемые цепочки. |
| `{ "bridge:call": { ... } }` | Вызывает функцию Нативного Моста (клиентского или серверного). |
| `{ "try": [...], "catch": [...] }` | Позволяет перехватывать и обрабатывать ошибки. |
| `{ "log": "msg" }` / `{ "log:value": "path" }` | **Декларативная отладка.** Выводит сообщение или значение переменной в консоль сервера. |
| `{ "auth:login": "userObj" }` / `{ "auth:logout": true }` | Управление сессией пользователя. |
| `{ "client:redirect": "'/path'" }` | SPA-навигация. |

---

## 🏛️ Интеллектуальный Нативный Мост

Мост — это единственный, строго контролируемый канал для взаимодействия с операционной системой.

**1. Клиентский Мост (UI ОС)**
Выполняется на клиенте. Может быть "fire-and-forget" или **интерактивным**.

*Пример Интерактивного Вызова:*
```javascript
"steps": [
  // 1. Остановить выполнение, показать диалог сохранения файла
  {
    "bridge:call": {
      "api": "dialogs.showSaveDialog",
      "args": { "defaultPath": "receipt.txt" },
      "await": true, // <--- Ключевой флаг
      "resultTo": "context.saveDialogResult"
    }
  },
  // 2. Этот шаг выполнится только ПОСЛЕ того, как пользователь закроет диалог
  {
    "if": "!context.saveDialogResult.canceled",
    "then": [
      { "log:value": "context.saveDialogResult.filePath" }
    ]
  }
]
```

**2. Серверный Мост (Кастомные Node.js Модули)**
Выполняется **немедленно на сервере**, может возвращать результат.

*Пример в `manifest.js`:*
```javascript
"bridge": {
  "custom": { "fileUtils": "file-utils.js" }
},
"routes": {
  "POST /save-file": {
    "type": "action",
    "steps": [{
      "bridge:call": {
        "api": "custom.fileUtils.saveTextFile",
        "args": "['./report.txt', body.text]",
        "resultTo": "context.saveResult"
      }
    }]
  }
}
```

## 🔬 Схемы Компонентов ("Контракты Данных")

Схемы позволяют LLM "видеть" требования каждого UI-компонента, что предотвращает ошибки несоответствия данных.

*Пример в `manifest/components.js`:*
```javascript
"receipt": { 
  "template": "receipt.html", 
  "style": "receipt.css",
  "schema": {
    // Валидатор проверит, что любой роут, использующий этот компонент,
    // предоставит коннектор 'receipt' в своей секции 'reads'.
    "requires": ["receipt"],
    // Эта часть служит документацией для LLM
    "variables": {
      "data.receipt.items": "Array<{name, price, quantity}>",
      "data.receipt.finalTotal": "String"
    }
  }
}
```

---

## 🔮 Будущие Направления

Проект достиг стабильной и мощной версии, открывая двери для дальнейшего расширения:

*   [ ] **Расширение API Нативного Моста:** Добавление поддержки принтеров (`printer.*`), системного трея, хранилища настроек (`store.*`).
*   [ ] **Интерактивный CLI:** Создание команд `axle-cli generate ...` для ускорения разработки.
*   [ ] **Система Автоматического Обновления:** Интеграция `electron-updater`.
*   [ ] **Выделенный Сайт с Документацией:** Создание полного руководства с примерами и лучшими практиками.

Этот проект — эксперимент в области будущего совместной работы человека и ИИ для создания надежных и предсказуемых программных систем. Вклад и идеи приветствуются.